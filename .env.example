# Root `.env` — database (Compose substitution). Copy to `.env` and adjust.
#
# Also create:
#   cp monitoring-service/.env.example monitoring-service/.env
#   cp notifications/.env.example notifications/.env

POSTGRES_DB=vdi
POSTGRES_USER=vdi
POSTGRES_PASSWORD=changeme
POSTGRES_PUBLISH_PORT=5433
