# Root: database (Compose reads this file automatically).
# Grafana / OpenStack for metrics → monitoring-service/.env
# Resend → notifications/.env

POSTGRES_DB=vdi
POSTGRES_USER=vdi
POSTGRES_PASSWORD=password
POSTGRES_PUBLISH_PORT=5433
