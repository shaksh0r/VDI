# Root .env — read by docker compose for ${\VAR} substitution (GITIGNORED).
# Keeps the OpenStack service-account credentials out of image layers and git.
OPENSTACK_USERNAME=sadik_vdi
OPENSTACK_PASSWORD=aeV9fo4UN6
